// && --- and
// || --- or
// ! --- reverse

let feesPaid = false
let emptyPocket = true

console.log(feesPaid && emptyPocket);
console.log(feesPaid || emptyPocket);
console.log(feesPaid != emptyPocket);
